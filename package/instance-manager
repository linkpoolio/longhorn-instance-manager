#!/bin/bash

function show_help() {
    cat <<EOF
Usage: $0 [OPTIONS]

Options:
    -s, --enable-spdk          Enable SPDK
        --spdk-interrupt-mode  Enable SPDK interrupt mode
        --spdk-memory-size     SPDK memory size in MB
        --spdk-cpumask         SPDK CPU mask
        --spdk-iobuf-large-pool-size  SPDK iobuf large_pool_count (0 = SPDK default)
        --spdk-no-hugepage     Disable SPDK hugepage usage
    -l, --spdk-log             SPDK log level
    -h, --help                 Show this help message and exit
EOF
    exit 0
}

function bind_dev() {
    mount --rbind /host/dev /dev
}

function bind_sys() {
    mount --rbind /host/sys /sys
}

function bind_lib_modules() {
    mount --rbind /host/lib/modules /lib/modules
}

function enable_tgtd() {
    echo "Enabling tgtd"
    tgtd -f 2>&1 | tee /var/log/tgtd.log &
}

function generate_nvme_hostid_and_hostnqn() {
    mkdir -p /etc/nvme

    local hostnqn=$(nvme gen-hostnqn)
    # hostnqn is generated from /sys/class/dmi/id/product_uuid according to the implementation of libnvme
    echo "$hostnqn" > /etc/nvme/hostnqn
    # Always generate the same hostid for the same hostnqn
    cat /sys/class/dmi/id/product_uuid > /etc/nvme/hostid
}

# prepare_spdk_startup_config writes an SPDK startup JSON config that sets the
# iobuf large buffer pool size (large_pool_count). The iobuf pool can ONLY be
# sized during spdk_tgt startup (iobuf_set_options is a startup-only RPC), which
# is why this is fed via --json instead of a runtime RPC. The NVMe-oF TCP
# transport is created later at runtime by the engine and draws from this pool;
#   $1: iobuf large_pool_count (> 1024)
#   $2: output config path
# Only emitted when the value exceeds SPDK's default iobuf large pool size
# (1024, IOBUF_DEFAULT_LARGE_POOL_SIZE); values <= 1024 keep the SPDK default.
function prepare_spdk_startup_config() {
    local large_pool_count="$1"
    local config_path="$2"

    cat > "$config_path" <<EOF
{
  "subsystems": [
    {
      "subsystem": "iobuf",
      "config": [
        {
          "method": "iobuf_set_options",
          "params": { "large_pool_count": ${large_pool_count} }
        }
      ]
    }
  ]
}
EOF
    echo "Generated SPDK startup config at ${config_path} (iobuf large_pool_count=${large_pool_count}):"
    cat "$config_path"
}

function enable_spdk_tgt() {
    local options=("$@")
    # Containerd spawns privileged containers with RLIMIT_MEMLOCK hard=8 MiB.
    # SPDK pins hugepage ranges as RDMA Memory Regions; with an 8 MiB cap it
    # can only register a sliver of the --mem-size pool and every IO whose
    # buffer lands outside the window serializes through a bounce buffer,
    # observed as ~400 ms per 128 KiB op on RDMA. The privileged container
    # already holds CAP_SYS_RESOURCE, so prlimit on our own shell raises the
    # hard cap to unlimited; spdk_tgt inherits it.
    prlimit --pid $$ --memlock=unlimited:unlimited 2>/dev/null || true
    # Crash forensics: the node's kernel.core_pattern is the plain relative
    # "core", so a dump lands in the process cwd -- launch spdk_tgt from /log
    # (the hostPath that survives pod replacement) instead of /, and pin the
    # core rlimit so a backtrace is available if it dies by signal. Hugepage
    # ranges are excluded from dumps by the default coredump_filter, so the
    # core stays heap+stacks sized.
    prlimit --pid $$ --core=unlimited:unlimited 2>/dev/null || true
    # --wait-for-rpc keeps spdk_tgt paused before subsystem initialisation so
    # that the Go side can call iobuf_set_options / bdev_nvme_set_options /
    # nvmf_create_transport with tuned opts; then it drives
    # framework_start_init to complete boot.
    options+=("--wait-for-rpc")
    echo "Enabling spdk_tgt with options: ${options[*]}"
    (cd /log && exec spdk_tgt "${options[@]}") 2>&1 | tee -a /log/spdk_tgt.log &

    timeout=120  # Timeout in seconds
    interval=1  # Interval in seconds
    elapsed_time=0

    while [ $elapsed_time -lt $timeout ]; do
        if [ -S "/var/tmp/spdk.sock" ]; then
            echo "Socket file '/var/tmp/spdk.sock' found after $elapsed_time seconds."
            return 0  # Exit successfully if the file exists
        fi

        sleep $interval
        elapsed_time=$((elapsed_time + interval))
    done

    echo "Timeout reached. Socket file '/var/tmp/spdk.sock' not found."
    return 1
}

enable_spdk=0
iobuf_large_pool_size=0
spdk_options=()
instance_manager_options=()

while [[ $# -gt 0 ]]; do
    opt="$1"
    case $opt in
        -s|--enable-spdk)
            enable_spdk=1
            ;;
        -m|--spdk-cpumask)
            spdk_options+=("-m" "$2")
            # Export so the IM Go process can derive accel_mlx5 num_requests
            # from the actual SPDK core count (not runtime.NumCPU(), which
            # returns the pod's cgroup-visible CPUs and can mismatch).
            export LONGHORN_V2_SPDK_CPUMASK="$2"
            shift
            ;;
        -s|--spdk-memory-size)
            spdk_options+=("--mem-size" "$2")
            # The engine derives its iobuf pool sizes and validates them
            # against this hugepage budget.
            export LONGHORN_V2_SPDK_MEM_SIZE_MIB="$2"
            shift
            ;;
        --spdk-iobuf-large-pool-size)
            iobuf_large_pool_size="$2"
            shift
            ;;
        -g|--spdk-no-hugepage)
            spdk_options+=("--no-huge")
            ;;
        -l|--spdk-log)
            spdk_options+=("-L" "$2")
            shift
            ;;
        --spdk-interrupt-mode)
            spdk_options+=("--interrupt-mode")
            ;;
        -h|--help)
            show_help
            ;;
        *)
            instance_manager_options+=("$1")
            ;;
    esac
    shift
done

#### Main ####

if [ "$enable_spdk" -eq 1 ]; then
    mkdir -p /log
    touch /log/instance-manager-${DATA_ENGINE}.log
    touch /log/spdk_tgt.log
fi

bind_dev
bind_sys
bind_lib_modules
[ "$enable_spdk" -eq 0 ] && enable_tgtd
[ "$enable_spdk" -eq 1 ] && generate_nvme_hostid_and_hostnqn

# When the iobuf large pool size is set above the SPDK default (1024), feed it to
# spdk_tgt via a startup JSON config. Values <= 1024 keep SPDK's built-in default,
# so nothing is generated and the launch is unchanged.
if [ "$enable_spdk" -eq 1 ] && [ "$iobuf_large_pool_size" -gt 1024 ]; then
    prepare_spdk_startup_config "$iobuf_large_pool_size" "/tmp/spdk-startup.json"
    spdk_options+=("--json" "/tmp/spdk-startup.json")
fi

[ "$enable_spdk" -eq 1 ] && enable_spdk_tgt "${spdk_options[@]}"

if [ "$enable_spdk" -eq 1 ]; then
    exec-logrotate
    exec longhorn-instance-manager "${instance_manager_options[@]}" 2>&1 | tee -a /log/instance-manager-${DATA_ENGINE}.log
else
    exec longhorn-instance-manager "${instance_manager_options[@]}"
fi
